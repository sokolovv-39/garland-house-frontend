export type EsWritingArrayType = {
  desc: string;
  keyValue: string;
};

export type LineType = {
  id: string;
  desc: string;
  unit: string;
  quantity: string;
  price: string;
  cost: string;
};
